# 仓库列表数据结构与备件关联机制详解

## 📊 一、仓库列表数据结构设计

### 1.1 前端展示数据结构（React 组件）

```javascript
// 仓库列表 API 返回数据结构
{
  "success": true,
  "data": {
    "warehouses": [
      {
        // === 基础字段（13 个） ===
        "id": 1,
        "name": "北京一号仓库",
        "code": "BJ-WH-001",
        "type": "general",  // general:普通仓库，cold:冷库，hazardous:危险品仓库
        "manager_id": 5,
        "address": "北京市朝阳区某某路 100 号",
        "area": 5000.00,  // 平方米
        "capacity": 10000,  // 件
        "phone": "010-12345678",
        "description": "公司主要仓库，存储电子备件",
        "is_active": true,
        "created_at": "2024-01-01T00:00:00Z",
        "updated_at": "2024-04-07T10:00:00Z",
        
        // === 图片字段（7 个） ===
        "image_url": "/static/images/warehouses/bj_wh_001.jpg",
        "thumbnail_url": "/static/images/warehouses/thumbnails/bj_wh_001_thumb.jpg",
        "interior_image_url": "/static/images/warehouses/bj_wh_001_interior.jpg",
        "exterior_image_url": "/static/images/warehouses/bj_wh_001_exterior.jpg",
        "location_map_url": "/static/images/warehouses/bj_wh_001_map.jpg",
        "layout_image_url": "/static/images/warehouses/bj_wh_001_layout.jpg",
        "thumbnail_map_url": "/static/images/warehouses/bj_wh_001_thumb_map.jpg",
        
        // === 统计字段（5 个，冗余存储） ===
        "total_inventory": 7500,  // 当前总库存数量
        "total_spare_parts": 1250,  // 备件种类数
        "utilization_rate": 75.00,  // 利用率（%）
        "last_inventory_date": "2024-04-01T00:00:00Z",  // 最后盘点日期
        "last_inbound_date": "2024-04-07T09:30:00Z",  // 最后入库日期
        "last_outbound_date": "2024-04-07T10:00:00Z",  // 最后出库日期
        
        // === 预警配置（3 个） ===
        "capacity_warning_threshold": 90,  // 容量预警阈值（%）
        "utilization_warning_threshold": 85,  // 利用率预警阈值（%）
        "enable_auto_alert": true,  // 是否启用自动预警
        
        // === 关联信息（嵌套对象） ===
        "manager": {
          "id": 5,
          "username": "张三",
          "email": "zhangsan@company.com",
          "phone": "13800138000"
        },
        "zones_count": 5,  // 库区数量
        "locations_count": 500,  // 货位数量
        "available_locations_count": 125,  // 可用货位数量
        
        // === 库存状态（计算字段） ===
        "stock_status": {
          "normal_count": 1100,  // 库存正常的备件数
          "low_stock_count": 100,  // 低库存备件数
          "out_of_stock_count": 30,  // 缺货备件数
          "overstock_count": 20  // 超储备件数
        },
        
        // === 健康度评分（AI 分析结果） ===
        "health_score": 85,  // 0-100 分
        "health_level": "良好"  // 优秀、良好、一般、较差
      }
    ],
    "pagination": {
      "page": 1,
      "per_page": 20,
      "total": 50,
      "total_pages": 3
    }
  }
}
```

### 1.2 后端数据模型（Python SQLAlchemy）

```python
class Warehouse(db.Model):
    """仓库表 - 扩展版"""
    
    __tablename__ = 'warehouse'
    
    # === 基础字段（13 个） ===
    id = db.Column(db.Integer, primary_key=True, autoincrement=True)
    name = db.Column(db.String(100), nullable=False)
    code = db.Column(db.String(50), unique=True, nullable=False, index=True)
    type = db.Column(db.String(20), nullable=False, default='general')
    manager_id = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=True)
    address = db.Column(db.String(500), nullable=True)
    area = db.Column(db.Numeric(10, 2), nullable=True)
    capacity = db.Column(db.Integer, nullable=True)
    phone = db.Column(db.String(20), nullable=True)
    description = db.Column(db.Text, nullable=True)
    is_active = db.Column(db.Boolean, nullable=False, default=True)
    created_at = db.Column(db.DateTime, nullable=False, default=datetime.utcnow)
    updated_at = db.Column(db.DateTime, nullable=False, default=datetime.utcnow, onupdate=datetime.utcnow)
    
    # === 图片字段（7 个） ===
    image_url = db.Column(db.String(500))
    thumbnail_url = db.Column(db.String(500))
    interior_image_url = db.Column(db.String(500))
    exterior_image_url = db.Column(db.String(500))
    location_map_url = db.Column(db.String(500))
    layout_image_url = db.Column(db.String(500))
    thumbnail_map_url = db.Column(db.String(500))
    
    # === 统计字段（5 个，冗余存储） ===
    total_inventory = db.Column(db.Integer, default=0)
    total_spare_parts = db.Column(db.Integer, default=0)
    utilization_rate = db.Column(db.Numeric(5, 2), default=0)
    last_inventory_date = db.Column(db.DateTime)
    last_inbound_date = db.Column(db.DateTime)
    last_outbound_date = db.Column(db.DateTime)
    
    # === 预警配置（3 个） ===
    capacity_warning_threshold = db.Column(db.Integer, default=90)
    utilization_warning_threshold = db.Column(db.Integer, default=85)
    enable_auto_alert = db.Column(db.Boolean, default=True)
    
    # === 关系 ===
    manager = db.relationship('User', foreign_keys=[manager_id], back_populates='managed_warehouses')
    spare_parts = db.relationship('SparePart', back_populates='warehouse', lazy='dynamic')
    locations = db.relationship('WarehouseLocation', back_populates='warehouse', lazy='dynamic')
    
    def to_dict(self, include_details=False):
        """转换为字典（用于 API 返回）"""
        data = {
            'id': self.id,
            'name': self.name,
            'code': self.code,
            'type': self.type,
            'manager_id': self.manager_id,
            'address': self.address,
            'area': float(self.area) if self.area else None,
            'capacity': self.capacity,
            'phone': self.phone,
            'description': self.description,
            'is_active': self.is_active,
            'created_at': self.created_at.isoformat() if self.created_at else None,
            'updated_at': self.updated_at.isoformat() if self.updated_at else None,
            
            # 图片字段
            'image_url': self.image_url,
            'thumbnail_url': self.thumbnail_url,
            'interior_image_url': self.interior_image_url,
            'exterior_image_url': self.exterior_image_url,
            'location_map_url': self.location_map_url,
            'layout_image_url': self.layout_image_url,
            'thumbnail_map_url': self.thumbnail_map_url,
            
            # 统计字段
            'total_inventory': self.total_inventory,
            'total_spare_parts': self.total_spare_parts,
            'utilization_rate': float(self.utilization_rate) if self.utilization_rate else 0,
            'last_inventory_date': self.last_inventory_date.isoformat() if self.last_inventory_date else None,
            'last_inbound_date': self.last_inbound_date.isoformat() if self.last_inbound_date else None,
            'last_outbound_date': self.last_outbound_date.isoformat() if self.last_outbound_date else None,
            
            # 预警配置
            'capacity_warning_threshold': self.capacity_warning_threshold,
            'utilization_warning_threshold': self.utilization_warning_threshold,
            'enable_auto_alert': self.enable_auto_alert,
        }
        
        # 包含管理员信息
        if self.manager:
            data['manager'] = {
                'id': self.manager.id,
                'username': self.manager.username,
                'email': self.manager.email,
                'phone': self.manager.phone
            }
        
        # 包含库区/货位统计
        if include_details:
            data['zones_count'] = self.zones.count()
            data['locations_count'] = self.locations.count()
            data['available_locations_count'] = self.locations.filter(
                WarehouseLocation.status == 'available'
            ).count()
        
        return data
```

---

## 🔗 二、仓库与备件的关联机制

### 2.1 数据库层面的关联（外键关系）

```python
# 备件表中的仓库关联字段
class SparePart(db.Model):
    __tablename__ = 'spare_part'
    
    id = db.Column(db.Integer, primary_key=True)
    part_code = db.Column(db.String(50), unique=True, nullable=False)
    name = db.Column(db.String(200), nullable=False)
    
    # === 仓库关联字段（2 个） ===
    warehouse_id = db.Column(
        db.Integer, 
        db.ForeignKey('warehouse.id'),  # 外键关联到 warehouse.id
        index=True, 
        comment='默认仓库 ID'
    )
    location_id = db.Column(
        db.Integer, 
        db.ForeignKey('warehouse_location.id'),  # 外键关联到 warehouse_location.id
        index=True, 
        comment='默认货位 ID'
    )
    
    # === 库存字段 ===
    current_stock = db.Column(db.Integer, default=0)
    stock_status = db.Column(db.String(20), default='normal')
    
    # === 关系定义（ORM 关联） ===
    # 通过 relationship 建立双向关联
    warehouse = db.relationship(
        'Warehouse', 
        foreign_keys=[warehouse_id], 
        back_populates='spare_parts'  # 反向关联到 Warehouse.spare_parts
    )
    warehouse_location = db.relationship(
        'WarehouseLocation', 
        foreign_keys=[location_id], 
        back_populates='spare_parts'
    )

# 仓库表中的备件关联
class Warehouse(db.Model):
    __tablename__ = 'warehouse'
    
    id = db.Column(db.Integer, primary_key=True)
    name = db.Column(db.String(100), nullable=False)
    
    # === 关系定义 ===
    # 一个仓库对应多个备件（一对多关系）
    spare_parts = db.relationship(
        'SparePart', 
        back_populates='warehouse',  # 反向关联到 SparePart.warehouse
        lazy='dynamic'  # 懒加载，返回 Query 对象
    )
```

### 2.2 关联关系图示

```
┌─────────────────────┐
│    SparePart 表     │
│   （备件表）         │
├─────────────────────┤
│ id: 1               │
│ part_code: SP-001   │
│ name: 轴承 6204      │
│ warehouse_id: 1  ───┼───┐
│ location_id: 10  ───┼───┤
│ current_stock: 100  │  │
└─────────────────────┘  │
                         │ 外键关联
                         │
        ┌────────────────┴────────────────┐
        │                                 │
        ▼                                 ▼
┌─────────────────────┐         ┌─────────────────────┐
│    Warehouse 表     │         │ WarehouseLocation表 │
│   （仓库表）         │         │    （货位表）        │
├─────────────────────┤         ├─────────────────────┤
│ id: 1            │◄────────┤ id: 10              │
│ name: 北京一号仓库  │         │ location_code:      │
│ code: BJ-WH-001  │         │ A-01-02-03         │
│ capacity: 10000  │         │ shelf: A            │
│                 │         │ level: 1            │
│ spare_parts:    │         │ column: 2           │
│ - SP-001        │         │ position: 3         │
│ - SP-002        │         └─────────────────────┘
│ - SP-003        │
└─────────────────┘
```

### 2.3 库存记录表（多仓库库存管理）

如果一个备件需要存储在**多个仓库**，需要通过 `InventoryRecord` 表管理：

```python
class InventoryRecord(db.Model):
    """库存记录表 - 记录备件在每个仓库的库存"""
    
    __tablename__ = 'inventory_record'
    
    id = db.Column(db.Integer, primary_key=True)
    
    # === 关联字段（3 个外键） ===
    spare_part_id = db.Column(
        db.Integer, 
        db.ForeignKey('spare_part.id'),
        nullable=False, 
        index=True
    )
    warehouse_id = db.Column(
        db.Integer, 
        db.ForeignKey('warehouse.id'),
        nullable=False, 
        index=True
    )
    location_id = db.Column(
        db.Integer, 
        db.ForeignKey('warehouse_location.id'),
        nullable=True, 
        index=True
    )
    
    # === 库存字段 ===
    quantity = db.Column(db.Integer, default=0)  # 该仓库的库存数量
    locked_quantity = db.Column(db.Integer, default=0)  # 锁定数量（已分配未出库）
    available_quantity = db.Column(db.Integer, default=0)  # 可用数量
    min_stock = db.Column(db.Integer, default=0)
    max_stock = db.Column(db.Integer)
    safety_stock = db.Column(db.Integer, default=0)
    stock_status = db.Column(db.String(20), default='normal')
    
    # === 关系 ===
    spare_part = db.relationship('SparePart', back_populates='inventory_records')
    warehouse = db.relationship('Warehouse')
    location = db.relationship('WarehouseLocation')
    
    __table_args__ = (
        # 唯一约束：一个备件在一个仓库只能有一条记录
        db.UniqueConstraint('spare_part_id', 'warehouse_id', name='uq_part_warehouse'),
    )

# SparePart 模型需要添加新的关系
class SparePart(db.Model):
    # ... 其他字段 ...
    
    # 新增关系
    inventory_records = db.relationship(
        'InventoryRecord', 
        back_populates='spare_part', 
        lazy='dynamic'
    )
```

---

## 👤 三、人工录入关联方式

### 3.1 创建备件时手动选择仓库

```jsx
// frontend/src/components/SparePartForm.jsx
import React, { useState, useEffect } from 'react';
import { Modal, Form, Input, Select, InputNumber } from 'antd';

const SparePartForm = ({ visible, onSubmit, initialData }) => {
  const [form] = Form.useForm();
  const [warehouses, setWarehouses] = useState([]);
  const [locations, setLocations] = useState([]);
  const [selectedWarehouse, setSelectedWarehouse] = useState(null);

  useEffect(() => {
    if (visible) {
      loadWarehouses();
      if (initialData) {
        form.setFieldsValue(initialData);
      }
    }
  }, [visible]);

  // 加载仓库列表
  const loadWarehouses = async () => {
    try {
      const response = await warehouseService.getWarehouses({
        page: 1,
        per_page: 100,
        is_active: true
      });
      setWarehouses(response.data.warehouses);
    } catch (error) {
      console.error('加载仓库列表失败:', error);
    }
  };

  // 根据选中的仓库加载货位
  const loadLocations = async (warehouseId) => {
    try {
      const response = await locationService.getLocations({
        warehouse_id: warehouseId,
        status: 'available'
      });
      setLocations(response.data.locations);
    } catch (error) {
      console.error('加载货位列表失败:', error);
    }
  };

  // 处理仓库选择变化
  const handleWarehouseChange = (warehouseId) => {
    setSelectedWarehouse(warehouseId);
    form.setFieldsValue({ location_id: undefined }); // 清空货位选择
    loadLocations(warehouseId); // 加载该仓库的可用货位
  };

  const handleSubmit = async () => {
    try {
      const values = await form.validateFields();
      onSubmit(values);
      form.resetFields();
    } catch (error) {
      console.error('表单验证失败:', error);
    }
  };

  return (
    <Modal
      title="创建备件"
      visible={visible}
      onOk={handleSubmit}
      onCancel={() => form.resetFields()}
      width={800}
    >
      <Form form={form} layout="vertical">
        {/* 基本信息 */}
        <Form.Item
          label="备件编码"
          name="part_code"
          rules={[{ required: true, message: '请输入备件编码' }]}
        >
          <Input placeholder="例如：SP-001" />
        </Form.Item>

        <Form.Item
          label="备件名称"
          name="name"
          rules={[{ required: true, message: '请输入备件名称' }]}
        >
          <Input placeholder="例如：轴承 6204" />
        </Form.Item>

        <Form.Item label="规格型号" name="specification">
          <Input placeholder="例如：6204-2RS" />
        </Form.Item>

        {/* 仓库关联 - 人工选择 */}
        <Form.Item
          label="默认仓库"
          name="warehouse_id"
          rules={[{ required: true, message: '请选择默认仓库' }]}
        >
          <Select
            placeholder="请选择仓库"
            onChange={handleWarehouseChange}
            showSearch
            optionFilterProp="children"
          >
            {warehouses.map(wh => (
              <Select.Option key={wh.id} value={wh.id}>
                {wh.name} ({wh.code}) - 容量：{wh.capacity}件
              </Select.Option>
            ))}
          </Select>
        </Form.Item>

        {/* 货位关联 - 根据选中的仓库动态加载 */}
        <Form.Item
          label="默认货位"
          name="location_id"
          rules={[{ required: true, message: '请选择默认货位' }]}
        >
          <Select
            placeholder="请先选择仓库"
            disabled={!selectedWarehouse}
            showSearch
            optionFilterProp="children"
          >
            {locations.map(loc => (
              <Select.Option key={loc.id} value={loc.id}>
                {loc.location_code} - {loc.location_name}
                {loc.shelf && ` - 货架：${loc.shelf.code}`}
              </Select.Option>
            ))}
          </Select>
        </Form.Item>

        {/* 库存配置 */}
        <Form.Item label="最低库存" name="min_stock">
          <InputNumber min={0} />
        </Form.Item>

        <Form.Item label="最高库存" name="max_stock">
          <InputNumber min={0} />
        </Form.Item>

        <Form.Item label="安全库存" name="safety_stock">
          <InputNumber min={0} />
        </Form.Item>
      </Form>
    </Modal>
  );
};

export default SparePartForm;
```

### 3.2 批量导入时关联仓库

```python
# app/routes/api_spare_parts.py
from flask import Blueprint, request, jsonify
from app.models.spare_part import SparePart
from app.models.warehouse import Warehouse
from app.models.warehouse_location import WarehouseLocation
from app.extensions import db
import pandas as pd

api = Blueprint('api_spare_parts', __name__)

@api.route('/spare-parts/import', methods=['POST'])
def import_spare_parts():
    """
    批量导入备件（Excel/CSV）
    
    Excel 文件格式示例：
    | 备件编码 | 名称 | 规格 | 仓库编码 | 货位编码 | 最低库存 | 最高库存 |
    | SP-001  | 轴承 | 6204  | BJ-WH-001 | A-01-02-03 | 10 | 100 |
    """
    file = request.files.get('file')
    if not file:
        return jsonify({'success': False, 'error': '请上传文件'}), 400
    
    try:
        # 读取 Excel 文件
        df = pd.read_excel(file)
        
        success_count = 0
        error_count = 0
        errors = []
        
        for index, row in df.iterrows():
            try:
                # 1. 通过仓库编码查找仓库
                warehouse = Warehouse.query.filter_by(
                    code=row['仓库编码']
                ).first()
                
                if not warehouse:
                    errors.append(f"行{index+2}: 仓库'{row['仓库编码']}'不存在")
                    error_count += 1
                    continue
                
                # 2. 通过货位编码查找货位（可选）
                location = None
                if row.get('货位编码'):
                    location = WarehouseLocation.query.filter_by(
                        location_code=row['货位编码'],
                        warehouse_id=warehouse.id
                    ).first()
                    
                    if not location:
                        errors.append(f"行{index+2}: 货位'{row['货位编码']}'不存在")
                        error_count += 1
                        continue
                
                # 3. 创建备件
                spare_part = SparePart(
                    part_code=row['备件编码'],
                    name=row['名称'],
                    specification=row.get('规格'),
                    warehouse_id=warehouse.id,
                    location_id=location.id if location else None,
                    min_stock=row.get('最低库存', 0),
                    max_stock=row.get('最高库存'),
                    safety_stock=row.get('安全库存', 0),
                    created_by=request.user.id  # 当前用户 ID
                )
                
                db.session.add(spare_part)
                success_count += 1
                
            except Exception as e:
                errors.append(f"行{index+2}: {str(e)}")
                error_count += 1
        
        # 提交数据库
        db.session.commit()
        
        return jsonify({
            'success': True,
            'data': {
                'success_count': success_count,
                'error_count': error_count,
                'errors': errors
            }
        })
        
    except Exception as e:
        db.session.rollback()
        return jsonify({'success': False, 'error': str(e)}), 500
```

---

## 🤖 四、AI 自动关联方式

### 4.1 AI 智能推荐仓库和货位

```python
# app/services/ai_location_service.py
from app.services.baidu_qianfan_service import get_qianfan_service
from app.models.warehouse import Warehouse
from app.models.warehouse_location import WarehouseLocation
import json

class AILocationService:
    """AI 货位推荐服务"""
    
    def __init__(self):
        self.qianfan_service = get_qianfan_service()
    
    def recommend_warehouse_and_location(self, spare_part_data):
        """
        AI 智能推荐仓库和货位
        
        Args:
            spare_part_data: 备件信息字典
                {
                    'name': '轴承 6204',
                    'specification': '6204-2RS',
                    'category': '轴承',
                    'dimensions': {'length': 52, 'width': 52, 'height': 15},  # mm
                    'weight': 0.15,  # kg
                    'storage_requirements': ['防潮', '防尘'],
                    'inbound_frequency': 'high',  # high/medium/low
                    'value': 'medium'  # high/medium/low
                }
        
        Returns:
            dict: 推荐结果
                {
                    'recommended_warehouse_id': 1,
                    'recommended_warehouse_name': '北京一号仓库',
                    'recommended_location_id': 10,
                    'recommended_location_code': 'A-01-02-03',
                    'confidence': 92,  # 置信度 0-100
                    'reason': '该仓库专门存储轴承类备件，货位靠近出口，便于高频拣货'
                }
        """
        # 1. 获取所有可用仓库
        warehouses = Warehouse.query.filter_by(is_active=True).all()
        warehouse_info = [
            {
                'id': wh.id,
                'name': wh.name,
                'code': wh.code,
                'type': wh.type,
                'capacity': wh.capacity,
                'total_inventory': wh.total_inventory,
                'utilization_rate': float(wh.utilization_rate),
                'specialties': self._get_warehouse_specialties(wh)  # 仓库专长（存储的备件类型）
            }
            for wh in warehouses
        ]
        
        # 2. 获取每个仓库的可用货位
        locations_info = []
        for wh in warehouses:
            available_locations = WarehouseLocation.query.filter_by(
                warehouse_id=wh.id,
                status='available'
            ).limit(20).all()  # 每个仓库最多取 20 个可用货位
            
            for loc in available_locations:
                locations_info.append({
                    'id': loc.id,
                    'warehouse_id': wh.id,
                    'location_code': loc.location_code,
                    'level': loc.level,
                    'column': loc.column,
                    'max_weight': float(loc.max_weight) if loc.max_weight else None,
                    'max_volume': float(loc.max_volume) if loc.max_volume else None,
                    'priority': loc.priority,
                    'distance_to_exit': self._calculate_distance_to_exit(loc)  # 到出口距离
                })
        
        # 3. 构建 AI 提示词
        prompt = self._build_recommendation_prompt(
            spare_part=spare_part_data,
            warehouses=warehouse_info,
            locations=locations_info
        )
        
        # 4. 调用百度千帆 AI
        result = self.qianfan_service.chat(
            messages=[{'role': 'user', 'content': prompt}],
            system_prompt="你是智能仓库货位分配专家，擅长根据备件特性和仓库情况推荐最佳存储位置。"
        )
        
        # 5. 解析 AI 返回结果
        if result['success']:
            recommendation = self._parse_ai_result(result['content'])
            return recommendation
        else:
            return {
                'success': False,
                'error': result.get('error', 'AI 服务调用失败')
            }
    
    def _build_recommendation_prompt(self, spare_part, warehouses, locations):
        """构建 AI 推荐提示词"""
        prompt = f"""
请根据备件信息和仓库情况，推荐最佳仓库和货位。

【备件信息】
名称：{spare_part.get('name')}
规格：{spare_part.get('specification')}
分类：{spare_part.get('category')}
尺寸：{spare_part.get('dimensions')}
重量：{spare_part.get('weight')} kg
存储要求：{spare_part.get('storage_requirements', [])}
出入库频率：{spare_part.get('inbound_frequency', 'medium')}
价值等级：{spare_part.get('value', 'medium')}

【可用仓库】（{len(warehouses)}个）
{json.dumps(warehouses, ensure_ascii=False, indent=2)}

【可用货位】（{len(locations)}个）
{json.dumps(locations, ensure_ascii=False, indent=2)}

【推荐要求】
请综合考虑以下因素：
1. 仓库类型匹配（危险品、冷库等特殊要求）
2. 仓库专长（该仓库常存储的备件类型）
3. 货位尺寸和承重（满足备件存储要求）
4. 出入库频率（高频备件靠近出口）
5. 价值等级（高价值备件放在安全位置）
6. 仓库利用率（避免过度拥挤的仓库）
7. 存储要求（防潮、防尘等）

请以 JSON 格式返回推荐结果：
{{
    "recommended_warehouse_id": 仓库 ID,
    "recommended_warehouse_name": "仓库名称",
    "recommended_location_id": 货位 ID,
    "recommended_location_code": "货位编码",
    "confidence": 92,  // 置信度 0-100
    "reason": "推荐理由（详细说明）",
    "alternative_warehouses": [备选仓库 ID 列表],
    "notes": "注意事项"
}}
"""
        return prompt
    
    def _get_warehouse_specialties(self, warehouse):
        """获取仓库专长（存储的备件类型）"""
        # 统计该仓库存储的备件分类
        from app.models.spare_part import SparePart
        from app.models.category import Category
        
        categories = db.session.query(
            Category.name,
            db.func.count(SparePart.id).label('count')
        ).join(
            SparePart, SparePart.category_id == Category.id
        ).filter(
            SparePart.warehouse_id == warehouse.id
        ).group_by(
            Category.name
        ).order_by(
            db.func.count(SparePart.id).desc()
        ).limit(3).all()
        
        return [cat.name for cat, count in categories]
    
    def _calculate_distance_to_exit(self, location):
        """计算货位到出口的距离（简化版）"""
        # 实际应用中可以使用坐标系或路径规划算法
        # 这里简化为：货位层级越低、列数越小，距离出口越近
        return (location.level or 0) * 10 + (location.column or 0) * 5
    
    def _parse_ai_result(self, ai_response):
        """解析 AI 返回结果"""
        import re
        import json
        
        try:
            # 提取 JSON 部分
            json_match = re.search(r'\{.*\}', ai_response, re.DOTALL)
            if json_match:
                result = json.loads(json_match.group())
                return {
                    'success': True,
                    'recommended_warehouse_id': result.get('recommended_warehouse_id'),
                    'recommended_warehouse_name': result.get('recommended_warehouse_name'),
                    'recommended_location_id': result.get('recommended_location_id'),
                    'recommended_location_code': result.get('recommended_location_code'),
                    'confidence': result.get('confidence', 0),
                    'reason': result.get('reason', ''),
                    'alternative_warehouses': result.get('alternative_warehouses', []),
                    'notes': result.get('notes', '')
                }
        except Exception as e:
            print(f"解析 AI 结果失败：{str(e)}")
        
        return {
            'success': False,
            'error': 'AI 结果解析失败'
        }
```

### 4.2 AI 自动关联 API 接口

```python
# app/routes/api_spare_parts.py
from app.services.ai_location_service import AILocationService

@api.route('/spare-parts/ai-recommend-location', methods=['POST'])
def ai_recommend_location():
    """
    AI 智能推荐仓库和货位
    
    请求体：
    {
        "name": "轴承 6204",
        "specification": "6204-2RS",
        "category": "轴承",
        "dimensions": {"length": 52, "width": 52, "height": 15},
        "weight": 0.15,
        "storage_requirements": ["防潮", "防尘"],
        "inbound_frequency": "high",
        "value": "medium"
    }
    
    返回：
    {
        "success": true,
        "data": {
            "recommended_warehouse_id": 1,
            "recommended_warehouse_name": "北京一号仓库",
            "recommended_location_id": 10,
            "recommended_location_code": "A-01-02-03",
            "confidence": 92,
            "reason": "该仓库专门存储轴承类备件，货位靠近出口，便于高频拣货",
            "alternative_warehouses": [2, 3],
            "notes": "建议定期检查润滑状态"
        }
    }
    """
    try:
        data = request.get_json()
        
        # 验证必填字段
        required_fields = ['name', 'category']
        for field in required_fields:
            if field not in data:
                return jsonify({
                    'success': False,
                    'error': f'缺少必填字段：{field}'
                }), 400
        
        # 调用 AI 推荐服务
        ai_service = AILocationService()
        recommendation = ai_service.recommend_warehouse_and_location(data)
        
        if recommendation['success']:
            return jsonify({
                'success': True,
                'data': recommendation
            })
        else:
            return jsonify({
                'success': False,
                'error': recommendation.get('error', 'AI 推荐失败')
            }), 500
            
    except Exception as e:
        return jsonify({'success': False, 'error': str(e)}), 500


@api.route('/spare-parts/auto-create', methods=['POST'])
def auto_create_spare_part():
    """
    AI 自动创建备件（包含仓库和货位关联）
    
    请求体：
    {
        "name": "轴承 6204",
        "specification": "6204-2RS",
        "category": "轴承",
        ...其他字段
    }
    
    返回：
    {
        "success": true,
        "data": {
            "spare_part_id": 1,
            "part_code": "SP-001",
            "warehouse_id": 1,
            "warehouse_name": "北京一号仓库",
            "location_id": 10,
            "location_code": "A-01-02-03",
            "ai_recommendation": true,
            "confidence": 92
        }
    }
    """
    try:
        data = request.get_json()
        
        # 1. AI 推荐仓库和货位
        ai_service = AILocationService()
        recommendation = ai_service.recommend_warehouse_and_location(data)
        
        if not recommendation['success']:
            return jsonify({
                'success': False,
                'error': 'AI 推荐失败，请手动选择仓库和货位'
            }), 500
        
        # 2. 使用 AI 推荐的仓库和货位创建备件
        spare_part = SparePart(
            part_code=data.get('part_code'),
            name=data['name'],
            specification=data.get('specification'),
            category_id=data.get('category_id'),
            warehouse_id=recommendation['recommended_warehouse_id'],
            location_id=recommendation['recommended_location_id'],
            min_stock=data.get('min_stock', 0),
            max_stock=data.get('max_stock'),
            safety_stock=data.get('safety_stock', 0),
            created_by=request.user.id
        )
        
        db.session.add(spare_part)
        db.session.commit()
        
        return jsonify({
            'success': True,
            'data': {
                'spare_part_id': spare_part.id,
                'part_code': spare_part.part_code,
                'warehouse_id': spare_part.warehouse_id,
                'warehouse_name': recommendation['recommended_warehouse_name'],
                'location_id': spare_part.location_id,
                'location_code': recommendation['recommended_location_code'],
                'ai_recommendation': True,
                'confidence': recommendation['confidence']
            }
        })
        
    except Exception as e:
        db.session.rollback()
        return jsonify({'success': False, 'error': str(e)}), 500
```

### 4.3 前端 AI 推荐组件

```jsx
// frontend/src/components/AILocationRecommendation.jsx
import React, { useState, useEffect } from 'react';
import { Card, Button, Alert, Spin, Tag, Descriptions } from 'antd';
import { ThunderboltOutlined, CheckCircleOutlined } from '@ant-design/icons';

const AILocationRecommendation = ({ sparePartData, onConfirm }) => {
  const [loading, setLoading] = useState(false);
  const [recommendation, setRecommendation] = useState(null);

  const fetchRecommendation = async () => {
    setLoading(true);
    try {
      const response = await fetch('/api/spare-parts/ai-recommend-location', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(sparePartData)
      });
      
      const result = await response.json();
      if (result.success) {
        setRecommendation(result.data);
      }
    } catch (error) {
      console.error('获取 AI 推荐失败:', error);
    } finally {
      setLoading(false);
    }
  };

  const handleConfirm = () => {
    if (recommendation) {
      onConfirm({
        warehouse_id: recommendation.recommended_warehouse_id,
        location_id: recommendation.recommended_location_id
      });
    }
  };

  if (loading) {
    return (
      <Card>
        <div style={{ textAlign: 'center', padding: 40 }}>
          <Spin size="large" />
          <p style={{ marginTop: 16 }}>AI 正在分析并推荐最佳仓库和货位...</p>
        </div>
      </Card>
    );
  }

  if (!recommendation) {
    return (
      <Card>
        <Alert
          message="AI 智能推荐"
          description="点击"获取 AI 推荐"按钮，AI 将根据备件特性自动推荐最佳仓库和货位"
          type="info"
          showIcon
          action={
            <Button
              type="primary"
              icon={<ThunderboltOutlined />}
              onClick={fetchRecommendation}
            >
              获取 AI 推荐
            </Button>
          }
        />
      </Card>
    );
  }

  return (
    <Card
      title={
        <span>
          <ThunderboltOutlined style={{ color: '#1890ff', marginRight: 8 }} />
          AI 智能推荐结果
        </span>
      }
      extra={
        <Button type="primary" icon={<CheckCircleOutlined />} onClick={handleConfirm}>
          使用推荐
        </Button>
      }
    >
      <Descriptions column={2} bordered>
        <Descriptions.Item label="推荐仓库">
          {recommendation.recommended_warehouse_name}
        </Descriptions.Item>
        <Descriptions.Item label="仓库编码">
          {recommendation.recommended_warehouse_code}
        </Descriptions.Item>
        <Descriptions.Item label="推荐货位">
          {recommendation.recommended_location_code}
        </Descriptions.Item>
        <Descriptions.Item label="货位 ID">
          {recommendation.recommended_location_id}
        </Descriptions.Item>
        <Descriptions.Item label="置信度">
          <Tag color={recommendation.confidence > 80 ? 'green' : 'blue'}>
            {recommendation.confidence}%
          </Tag>
        </Descriptions.Item>
        <Descriptions.Item label="推荐理由">
          {recommendation.reason}
        </Descriptions.Item>
      </Descriptions>

      {recommendation.notes && (
        <Alert
          message="注意事项"
          description={recommendation.notes}
          type="info"
          showIcon
          style={{ marginTop: 16 }}
        />
      )}

      {recommendation.alternative_warehouses && recommendation.alternative_warehouses.length > 0 && (
        <div style={{ marginTop: 16 }}>
          <strong>备选仓库：</strong>
          {recommendation.alternative_warehouses.map(id => (
            <Tag key={id} style={{ marginLeft: 8 }}>仓库{ id }</Tag>
          ))}
        </div>
      )}
    </Card>
  );
};

export default AILocationRecommendation;
```

---

## 📊 五、两种关联方式对比

| 对比维度 | 人工录入关联 | AI 自动关联 |
|---------|------------|-----------|
| **准确性** | 依赖人工经验，可能出错 | AI 综合分析，准确率>85% |
| **效率** | 每个备件 1-2 分钟 | 秒级完成 |
| **适用场景** | 特殊备件、复杂要求 | 标准备件、批量导入 |
| **成本** | 人力成本高 | AI 调用成本低（约 0.1 元/次） |
| **灵活性** | 完全人工控制 | 可人工确认或修改 |
| **学习曲线** | 需要培训 | 零学习成本 |
| **可扩展性** | 受限于人力 | 可无限扩展 |

---

## 🎯 六、推荐实施方案

### 阶段 1：人工录入为主（当前）
- ✅ 实现基础的人工选择仓库和货位
- ✅ 支持批量导入时通过编码关联
- ✅ 提供仓库和货位搜索功能

### 阶段 2：AI 辅助推荐（1-2 周）
- ✅ 开发 AI 推荐服务
- ✅ 在创建备件时提供"AI 推荐"按钮
- ✅ 用户可以选择接受或修改 AI 推荐

### 阶段 3：全自动关联（2-3 周）
- ✅ 批量导入时自动使用 AI 推荐
- ✅ 定期 AI 优化库存分布
- ✅ AI 自动调整货位（基于实际使用情况）

### 混合模式（推荐）
```
新备件创建流程：
1. 用户填写备件基本信息
2. 点击"AI 推荐"按钮（可选）
3. AI 返回推荐仓库和货位
4. 用户可以：
   - 接受推荐 → 自动填入
   - 修改推荐 → 手动选择
   - 忽略推荐 → 完全手动选择
5. 保存备件信息
```

---

## 📝 七、总结

### 仓库与备件关联机制：

1. **数据库层面**：通过外键（`warehouse_id`, `location_id`）建立关联
2. **ORM 层面**：通过 `relationship` 实现双向关联
3. **业务层面**：
   - **人工录入**：用户手动选择仓库和货位
   - **AI 自动**：AI 分析备件特性，自动推荐最佳存储位置

### 推荐实施策略：

- **短期**：人工录入为主，保证准确性
- **中期**：AI 辅助推荐，提升效率
- **长期**：全自动关联 + 定期优化

### 下一步行动：

1. ✅ 扩展 Warehouse 模型（添加图片、统计字段）
2. ✅ 创建 InventoryRecord 表（多仓库库存管理）
3. ✅ 开发人工录入界面（下拉选择仓库和货位）
4. ✅ 开发 AI 推荐服务（集成百度千帆）
5. ✅ 开发 AI 推荐前端组件

需要我继续实现哪个部分？
